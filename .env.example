# Local dev default - points at the backend running via `docker compose up` or
# `mvnw spring-boot:run` on your own machine. In production, set this to your
# self-hosted backend's public URL (e.g. https://api.yourdomain.com).
VITE_API_BASE_URL=http://localhost:8080
